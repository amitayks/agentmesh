[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# No HTTP healthcheck - relay is WebSocket only
# Docker HEALTHCHECK (nc -z localhost 8765) handles this
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
