# AgentMesh Relay Server Environment Variables

# Server configuration
RELAY_ADDR=0.0.0.0:${PORT:-8765}
RUST_LOG=agentmesh_relay=info

# Railway provides PORT automatically
# The relay will bind to 0.0.0.0:$PORT
