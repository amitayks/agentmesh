# AgentMesh Registry API Environment Variables

# Database (Railway provides DATABASE_URL automatically when you add PostgreSQL)
DATABASE_URL=postgres://user:password@host:5432/agentmesh

# Server configuration (Railway provides PORT automatically)
HOST=0.0.0.0
PORT=${PORT:-8080}

# Logging
RUST_LOG=agentmesh_registry=info,actix_web=info
